---

resources:
  build/native-build-tools:
    phases: install
    version: latest
    path: $ROTOOL_DIR

  build/root-env:
    phases: install
    version: latest
    path: $ROBUILD_DIR/Env

exports:
  docs/prminxml-examples:
    phases: install
    newer: true
    path: $INSTALL_DIR/Library/docs/
