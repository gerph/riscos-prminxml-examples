---

resources:
  build/native-build-tools:
    phases: install
    version: latest
    path: $ROTOOL_DIR

  build/root-env:
    phases: install
    version: latest
    path: $ROBUILD_DIR/Env

#exports:
